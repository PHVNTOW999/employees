import React from 'react';
import Layout from "../../components/layout";

const Login = () => {
    return (
        <div>
            <Layout>
                Login
            </Layout>
        </div>
    );
};

export default Login;
